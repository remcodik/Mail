# Secrets & auth — never commit (Sprint 2+)
.env
credentials.json
token.json
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# local SQLite dev DBs (prod DB lives on a mounted volume)
*.db
/data/
