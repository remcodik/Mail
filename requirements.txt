# --- core backend (needed for demo + live) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- live mode only (real Gmail + Claude); demo mode works without these ---
anthropic>=0.40
google-api-python-client>=2.100
google-auth>=2.30
google-auth-oauthlib>=1.2
python-dotenv>=1.0
